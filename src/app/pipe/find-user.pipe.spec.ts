import { FindUserPipe } from './find-user.pipe';

describe('FindUserPipe', () => {

  it('create an instance - test 9', () => {
    const pipe = new FindUserPipe();
    expect(pipe).toBeTruthy();
  });
});
